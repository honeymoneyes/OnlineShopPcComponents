1. Почитать больше практик DIP.


Изменения за сегодня:

1. Создан общий класс для компонентов во избежание дублирования кода, в который перемещены общие поля всех классов.
2. Дописана логика каждой навигационной ссылки.
3. Изменена ошибка в модели ( реализован не тот компонент ).
4. Изменена структура папки models.
5. Создан интерфейс ProductService для реализации абстракции зависимостей.
6. Изменена структура папки services.
7. Изменена логика работы контроллера:

   a. Создан класс PageNameValidator, который включает в себя методы проверки входящей страницы.
   b. Метод сравнивает входящую страницу с существующими и перенаправляет. Этот метод создан во избежание
   дублирования кода методов контроллера для каждой страницы, используя общий шаблон.
   c. Создан enum для упрощения сравнения объекта, полученного из представления и существующих объектов.

8. Создан класс ModelUtils, который упрощает взаимодействие с Model. Создан отдельный метод с дополнительными
   параметрами для фотографий продуктов. Принимает на вход не жестко связанный объект, а его абстракцию - интерфейс,
   который реализуют эти объекты ( DIP ).
